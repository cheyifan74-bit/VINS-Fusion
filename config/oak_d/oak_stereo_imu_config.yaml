%YAML:1.0

#common parameters
#support: 1 imu 1 cam; 1 imu 2 cam: 2 cam; 
imu: 1         
num_of_cam: 2  

imu_topic: "/stereo_depth_imu/imu/data"
image0_topic: "/cam0/image_raw"
image1_topic: "/cam1/image_raw"
output_path: "/home/aa/VINS_FUSION_WS/output/"

cam0_calib: "oak_left.yaml"
cam1_calib: "oak_right.yaml"
fisheye_mask: "fisheye_mask_640x400.jpg"
image_width: 640
image_height: 400
   

# Extrinsic parameter between IMU and Camera.
estimate_extrinsic: 1   # 0  Have an accurate extrinsic parameters.
                        # 1  Have an initial guess about extrinsic parameters. We will optimize around your initial guess.
                        # 2  don't know extrinsic parameters. We will estimate them online.

body_T_cam0: !!opencv-matrix
   rows: 4
   cols: 4
   dt: d
   data: [0.0,  1.0,  0.0,  -0.00079,
          1.0,  0.0,  0.0,  -0.075445,
          0.0,  0.0, -1.0,  -0.008,
          0.0,  0.0,  0.0,   1.0]

body_T_cam1: !!opencv-matrix
   rows: 4
   cols: 4
   dt: d
   data: [0.0,  1.0,  0.0,  -0.00079,
          1.0,  0.0,  0.0,  -0.000445,
          0.0,  0.0, -1.0,  -0.008,
          0.0,  0.0,  0.0,   1.0]

#Multiple thread support
multiple_thread: 1

#feature traker paprameters
max_cnt: 150            # max feature number in feature tracking
min_dist: 20            # min distance between two features 
freq: 10                # frequence (Hz) of publish tracking result. At least 10Hz for good estimation. If set 0, the frequence will be same as raw image 
F_threshold: 1.5        # ransac threshold (pixel)
show_track: 1           # publish tracking image as topic
flow_back: 1            # perform forward and backward optical flow to improve feature tracking accuracy

# ZUPT stationary detection parameters
zupt_max_disparity: 5.0 
zupt_max_acc_var: 0.01     # max accelerometer norm variance (Welford)
zupt_max_gyr_var: 0.001   # max gyroscope norm variance (Welford)

#optimization parameters
max_solver_time: 0.04  # max solver itration time (ms), to guarantee real time
max_num_iterations: 8   # max solver itrations, to guarantee real time
keyframe_parallax: 10.0 # keyframe selection threshold (pixel)

#imu parameters       The more accurate parameters you provide, the better performance
acc_n: 0.1         # accelerometer measurement noise standard deviation. (BMI270)
gyr_n: 0.01        # gyroscope measurement noise standard deviation. (BMI270)
acc_w: 0.001       # accelerometer bias random work noise standard deviation.  
gyr_w: 0.0001      # gyroscope bias random work noise standard deviation.     
g_norm: 9.81007     # gravity magnitude

#unsynchronization parameters
estimate_td: 0                      # online estimate time offset between camera and imu
td: 0.01                             # initial value of time offset. unit: s. readed image clock + td = real image clock (IMU clock)

#loop closure parameters
load_previous_pose_graph: 0        # load and reuse previous pose graph; load from 'pose_graph_save_path'
pose_graph_save_path: "/home/aa/VINS_FUSION_WS/output/pose_graph/" # save and load path
save_image: 1                   # save image in pose graph for visualization prupose; you can close this function by setting 0 
